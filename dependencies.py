"""Dependency declarations for the package wrapper.

These packages are required by the existing core converter. This module does
not install them automatically.
"""

REQUIRED_PIP_PACKAGES = ["lxml", "reportlab", "PyMuPDF", "Pillow"]
